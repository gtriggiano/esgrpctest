import should from 'should/as-function'

import BackendInterface from './BackendInterface'

describe('BackendInterface', function () {
  it('should be a function', () => { should(BackendInterface).be.a.Function() })
})
